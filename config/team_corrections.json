{
  "_note": "Static team corrections. Committed to git. Read-only at runtime — add entries here for platform-wide facts that should survive a data/ wipe. Publisher/advertiser-specific knowledge belongs in data/entity_overrides.json (managed autonomously by Scout via record_entity_note).",
  "corrections": []
}
