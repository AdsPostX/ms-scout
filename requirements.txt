# Exact pins — a deploy must be a pure function of the commit. These are the
# versions production resolved on 2026-07-09 (Render build log, deploy of cc37dcb);
# this freeze changed zero behavior. Bump deliberately: one-concern PR, read the
# changelog delta, run smoke_test.py, watch the deploy.
requests==2.34.2
google-api-python-client==2.198.0
google-auth-oauthlib==1.4.0
python-dotenv==1.2.2
clickhouse-connect==0.15.1
anthropic==0.116.0
latitude-telemetry==1.0.0
latitude-sdk==5.10.0
slack-sdk==3.43.0
pytz==2026.2
upstash-redis==1.7.0
# Transitive via google-api-core; must stay ≥5.29.6 (GHSA-7gcm-g887-7qv7 / CVE-2026-0994).
protobuf==6.33.6
# scout_attachments.py — Sheets CSV + uploaded CSV parsing + markdown summary
pandas==2.3.3
tabulate==0.10.0
# scout_attachments.py — pdftotext CLI fallback if poppler-utils not installed at runtime
pdfplumber==0.11.10
# scout_attachments.py — Excel + Word format support (closes the .xlsx / .docx
# gaps revealed in live testing on PR #236 + closes the latent .xls misrouting bug)
openpyxl==3.1.5
python-docx==1.2.0
xlrd==2.0.2
