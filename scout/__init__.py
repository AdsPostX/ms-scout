"""scout — the domain-package tree DESIGN.md's target structure describes.

This is a partial start, not the full reorg: only scout/monitoring/ (the
demand-feed monitor daemons, extracted out of demand_feed_main.py) and
scout/offers/ (the affiliate-network scraper, moved from offer_scraper.py)
exist so far. The rest of DESIGN.md's target tree (scout/publishers/,
scout/campaigns/, scout/revenue/, scout/slack/, scout/agent/, scout/routing/,
scout/entrypoint/) is a separate, larger effort — not attempted here.
"""
